
@{
    ViewData["Title"] = "Yetkiniz Yok";
    Layout = null;
}




<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erişim Reddedildi - 403</title>
    <style>
        :root {
            /* Marka Rengini Buradan Değiştirebilirsin */
            --primary-color: #0d6efd; /* Bootstrap Primary Blue */
            --secondary-color: #6c757d;
            --bg-color: #f3f4f6;
            --text-color: #1f2937;
            --card-bg: #ffffff;
        }

        body {
            background-color: var(--bg-color);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-color);
        }

        .error-container {
            background: var(--card-bg);
            padding: 3rem;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.05);
            max-width: 500px;
            width: 90%;
            text-align: center;
            border-top: 5px solid var(--primary-color);
            position: relative;
        }

        .lock-icon {
            width: 80px;
            height: 80px;
            background: #eef2ff;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: var(--primary-color);
        }

        /* Basit CSS Kilit İkonu */
        .lock-body {
            width: 24px;
            height: 20px;
            border: 3px solid var(--primary-color);
            border-radius: 4px;
            position: relative;
            top: 5px;
        }

            .lock-body:before {
                content: '';
                position: absolute;
                width: 14px;
                height: 12px;
                border: 3px solid var(--primary-color);
                border-bottom: none;
                border-radius: 10px 10px 0 0;
                top: -15px;
                left: 2px;
            }

            .lock-body:after {
                content: '';
                position: absolute;
                width: 4px;
                height: 4px;
                background: var(--primary-color);
                border-radius: 50%;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
            }

        h1 {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            color: #111;
        }

        .status-code {
            font-size: 0.875rem;
            font-weight: 600;
            letter-spacing: 1px;
            color: var(--secondary-color);
            text-transform: uppercase;
            margin-bottom: 1.5rem;
            display: block;
        }

        p {
            color: var(--secondary-color);
            line-height: 1.6;
            margin-bottom: 2rem;
            font-size: 1rem;
        }

        .actions {
            display: flex;
            gap: 15px;
            justify-content: center;
        }

        .btn {
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 6px;
            font-weight: 500;
            font-size: 0.95rem;
            transition: all 0.2s ease;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
            border: 1px solid var(--primary-color);
        }

            .btn-primary:hover {
                background-color: #0b5ed7;
                box-shadow: 0 4px 12px rgba(13, 110, 253, 0.2);
            }

        .btn-outline {
            background-color: transparent;
            color: var(--secondary-color);
            border: 1px solid #dee2e6;
        }

            .btn-outline:hover {
                background-color: #f8f9fa;
                color: #000;
                border-color: #cdd4da;
            }

        .error-footer {
            margin-top: 2rem;
            font-size: 0.75rem;
            color: #adb5bd;
            border-top: 1px solid #f0f0f0;
            padding-top: 15px;
        }

    </style>
</head>
<body>

    <div class="error-container">
        <div class="lock-icon">
            <div class="lock-body"></div>
        </div>

        <span class="status-code">Hata Kodu: 403</span>
        <h1>Erişim Yetkiniz Yok</h1>

        <p>
            Bu sayfayı görüntülemek için gerekli izinlere sahip değilsiniz.
            Yönetici hesabıyla giriş yapmanız veya sistem yöneticinizle
            iletişime geçmeniz gerekebilir.
        </p>

        <div class="actions">
            <a href="/Login/Index" class="btn btn-outline">Geri Dön</a>

            <a href="/Default/Index" class="btn btn-primary">Anasayfaya Git</a>
        </div>

        <div class="error-footer">
            Request ID: @Context.TraceIdentifier <br>
            @DateTime.Now.ToString("dd.MM.yyyy HH:mm")
        </div>
    </div>

</body>
</html>